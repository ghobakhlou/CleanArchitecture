using Application.Student;

namespace Api
{
    /// <summary>
    /// Entry point for GraphQL mutations. Individual mutations are generated as partial members
    /// via the Motion-First PCD CLI so cross-cutting changes stay centralized.
    /// </summary>
    public partial class Mutation
    {
    }
}
